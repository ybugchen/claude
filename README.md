# 📝 Todo List Demo App

一个简单的 Todo List 演示应用，用于展示 Claude Code 的功能。

## ✨ 功能特性

- ✅ 添加、编辑、删除待办事项
- ✅ 标记任务完成状态
- ✅ 实时统计显示（总计、待完成、已完成）
- ✅ 响应式设计，支持移动端
- ✅ RESTful API 架构
- ✅ 完整的单元测试

## 🛠️ 技术栈

### 后端
- **Node.js** - JavaScript 运行时
- **Express** - Web 框架
- **CORS** - 跨域资源共享

### 前端
- **Vanilla JavaScript** - 原生 JS
- **HTML5 & CSS3** - 现代化 UI

### 开发工具
- **Jest** - 测试框架
- **Supertest** - API 测试
- **ESLint** - 代码检查
- **Nodemon** - 开发热重载

## 📦 安装

```bash
# 安装依赖
npm install
```

## 🚀 运行

### 开发模式（带热重载）
```bash
npm run dev
```

### 生产模式
```bash
npm start
```

服务器将在 http://localhost:3000 启动

## 🧪 测试

```bash
# 运行所有测试
npm test

# 运行测试并查看覆盖率
npm test -- --coverage
```

## 📚 API 文档

### 获取所有待办事项
```http
GET /api/todos
```

**响应示例:**
```json
[
  {
    "id": 1,
    "title": "学习 Claude Code",
    "completed": false,
    "createdAt": "2026-02-05T09:00:00.000Z"
  }
]
```

### 获取单个待办事项
```http
GET /api/todos/:id
```

### 创建待办事项
```http
POST /api/todos
Content-Type: application/json

{
  "title": "新任务"
}
```

### 更新待办事项
```http
PUT /api/todos/:id
Content-Type: application/json

{
  "title": "更新后的标题",
  "completed": true
}
```

### 删除待办事项
```http
DELETE /api/todos/:id
```

### 健康检查
```http
GET /api/health
```

## 📁 项目结构

```
todo-demo-app/
├── __tests__/              # 测试文件
│   └── server.test.js      # API 测试
├── public/                 # 前端静态文件
│   ├── index.html          # HTML 页面
│   ├── styles.css          # 样式表
│   └── app.js              # 前端逻辑
├── src/                    # 后端源代码
│   ├── data/
│   │   └── store.js        # 数据存储（内存）
│   ├── routes/
│   │   └── todos.js        # Todo 路由
│   └── server.js           # 主服务器文件
├── .eslintrc.json          # ESLint 配置
├── .gitignore              # Git 忽略文件
├── jest.config.js          # Jest 配置
├── package.json            # 项目配置
└── README.md               # 项目文档
```

## 🎯 使用 Claude Code 演示的功能

这个项目可以用来演示 Claude Code 的以下功能：

1. **代码阅读和理解** - 让 Claude 解释代码结构
2. **添加新功能** - 例如添加任务优先级、截止日期等
3. **重构代码** - 优化代码结构和性能
4. **修复 Bug** - 找出并修复问题
5. **编写测试** - 添加更多测试用例
6. **代码审查** - 检查代码质量和最佳实践
7. **生成文档** - 自动生成 API 文档

## 🔧 代码检查

```bash
# 运行 ESLint
npm run lint
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

使用 Claude Code 构建 - Anthropic 的官方 CLI 工具
